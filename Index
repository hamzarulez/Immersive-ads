import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Mail } from "lucide-react";

export default function LandingPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-900 via-purple-900 to-pink-800 text-white flex flex-col items-center justify-center p-6">
      {/* Hero Section */}
      <motion.div
        initial={{ opacity: 0, y: -30 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.8 }}
        className="text-center max-w-3xl"
      >
        <h1 className="text-5xl font-extrabold mb-4 tracking-tight">
          Immersive Ads
        </h1>
        <p className="text-lg md:text-xl text-gray-200 mb-6">
          The marketplace that connects <span className="text-pink-300 font-semibold">brands</span> with <span className="text-indigo-300 font-semibold">game creators</span> for impactful in-game advertising.
        </p>
        <Button className="bg-pink-500 hover:bg-pink-600 text-lg px-6 py-3 rounded-2xl shadow-lg">
          Get Started
        </Button>
      </motion.div>

      {/* Explainer Section */}
      <motion.div
        initial={{ opacity: 0, y: 30 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.3, duration: 0.8 }}
        className="grid md:grid-cols-2 gap-8 mt-16 max-w-5xl"
      >
        <Card className="bg-white/10 backdrop-blur-md rounded-2xl shadow-xl">
          <CardContent className="p-6">
            <h2 className="text-2xl font-bold mb-2">For Brands</h2>
            <p className="text-gray-200">
              Launch immersive campaigns in games and reach engaged audiences with native, interactive experiences.
            </p>
          </CardContent>
        </Card>

        <Card className="bg-white/10 backdrop-blur-md rounded-2xl shadow-xl">
          <CardContent className="p-6">
            <h2 className="text-2xl font-bold mb-2">For Creators</h2>
            <p className="text-gray-200">
              Monetize your games by hosting ad placements seamlessly integrated into gameplay.
            </p>
          </CardContent>
        </Card>
      </motion.div>

      {/* Call to Action */}
      <motion.div
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.6, duration: 0.8 }}
        className="mt-20 w-full max-w-lg text-center"
      >
        <h3 className="text-2xl font-semibold mb-4">Join Early</h3>
        <p className="text-gray-300 mb-6">Be the first to know when we launch the platform.</p>
        <div className="flex items-center gap-2">
          <Input type="email" placeholder="Enter your email" className="bg-white/20 text-white placeholder-gray-300" />
          <Button className="bg-indigo-500 hover:bg-indigo-600 flex items-center gap-2">
            <Mail className="w-4 h-4" /> Notify Me
          </Button>
        </div>
      </motion.div>

      {/* Footer */}
      <footer className="mt-24 text-sm text-gray-400">
        © {new Date().getFullYear()} Immersive Ads. All rights reserved.
      </footer>
    </div>
  );
}
